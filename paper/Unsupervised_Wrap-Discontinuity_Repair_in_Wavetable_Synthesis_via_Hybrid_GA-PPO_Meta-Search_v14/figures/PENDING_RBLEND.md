# Pending $R_{\mathrm{blend}}$ notes (v13)

**Status:** Deferred D1 matched-$5$k re-search is **complete**.

Canonical numbers:

| Artifact | Role |
|----------|------|
| `meta_approaches_table.tex` | 3-seed mean±std under $R_{\mathrm{blend}}{+}J$ |
| `meta_approach_compare_v13_seed1.json` | Seed `1902771841` trajectories |
| `multiseed_summary.json` | Holdout + search aggregates |
| `param_fluctuation.json` | Champion size swings across search |

Do not revert Table `tab:meta-approaches` to the historical prolonged-$R$ cheap re-fit.
