\documentclass[11pt,a4paper]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{setspace}
\setstretch{1.07}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=blue!50!black,
  urlcolor=blue!60!black
}

\title{\textbf{DenoiseOpt v2:} Residual Scoring and Bi-Level\\
Meta-Learning for Wavetable Wrap Crackle}
\author{
  Julian~M.~Kleber\\
  ORCID: \href{https://orcid.org/0000-0001-5518-0932}{0000-0001-5518-0932}\\
  Email: \href{mailto:julian.m.kleber@gmail.com}{julian.m.kleber@gmail.com}\\[0.6em]
  \small
  Synth:
  \href{https://github.com/reeldemo/reelsynth}{github.com/reeldemo/reelsynth}\\
  Meta artifacts:
  \href{https://github.com/reeldemo/denoise-opt-meta}{github.com/reeldemo/denoise-opt-meta}
}
\date{18 July 2026 \\ \small Paper version \texttt{v2} (residual + nested loss opt)}

\begin{document}
\maketitle

\begin{abstract}
Open wrap seams in periodic wavetable cycles inject audible crackle under cyclic
playback. DenoiseOpt is a seam-local bake stack $f_\theta$, $\theta\in[0,1]^{12}$,
scored without paired clean labels. This revision replaces wrap-energy quality
$Q$ as the \emph{outer} meta objective with a prolonged \textbf{residual score}
$\in[0,1]$ (1~=~best): the engine's tiled DenoiseOpt cycle is compared to an ideal
multi-period reference from the same seed with open-wrap cliffs withheld.
The outer loop also nests an \textbf{inner unsupervised loss} fit
$L=(1-\mathcal{D})+\lambda(1-\mathcal{S})$ over $\theta$ (and optionally $\lambda$).
Across \textbf{1500} literature-informed trials (seven prior families including
explicit bi-level loss opt), Meta Top~1 \texttt{evo\_explore\_515} reaches
residual $\approx 0.824$ vs naive DualCosine $\approx 0.698$ on held-out
validation ($N{=}2000$)---a clear $+0.126$ margin---while keeping
$\mathcal{S}\approx 1.0$. Nested loss-opt priors were searched but did not dominate
the residual elite set; wide evolutionary mutation did.
\end{abstract}

\section{Introduction}
Wavetable synthesizers store one-period frames and wrap phase modulo one.
Discontinuous ends produce periodic broadband clicks. Classical DualCosine fades
and live BLEP address related phenomena; edited Quant frames still need a fast
bake operator that is aggressive on wrap energy and conservative on mid-cycle
timbre.

\paragraph{Why residual?}
Wrap-energy proxies $\mathcal{D},\mathcal{S}$ and $Q=\tfrac12(\mathcal{D}+\mathcal{S})$
(v1) reward closing the seam, but do not ask whether prolonged cyclic playback
matches the intended continuous wave. Residual scoring constructs that reference
explicitly and ranks algorithms by how little ``noise'' remains after bake.

\paragraph{Contributions (v2).}
(1)~Prolonged residual score in $[0,1]$ with known ideal vs engine tiling.
(2)~Bi-level meta: inner coordinate descent on $L$, outer maximize residual.
(3)~1500-trial search with seven priors, including \texttt{bilevel\_loss}.
(4)~Clear residual win over DualCosine; honest report that loss-opt elites lost
to evolutionary explore on this objective.
(5)~Frozen $\theta^\star$ locked from champion \texttt{evo\_explore\_515}.

\section{Related work and literature triage}
Literature was harvested earlier via Klaut Research providers (OpenAlex / Crossref /
arXiv). We keep the same used / screened split as v1.

\subsection{Used}
VA anti-aliasing and discontinuity control~\cite{valimaki2006va,esqueda2016aliasing,esqueda2016blamp};
differentiable wavetable / DDSP context~\cite{shan2021dwt}; Noise2Noise-style
label-free restoration philosophy~\cite{lehtinen2018n2n}; Bayesian HPO and
surveys~\cite{snoek2012bo,bischl2023hpo}; PBT-style exploit/mutate; irace-style
racing~\cite{lopez2016irace}; MOEA/D multi-objective cues~\cite{zhang2007moead}.

\subsection{Screened}
Medical imaging / federated / prompting surveys; Adam as ambient optimizer only;
MAML~\cite{finn2017maml} inspirational but not used (no task-gradient through a
deep net).

\section{Residual score}
Let $r$ be a procedural engine cycle (\texttt{generate\_sound}, may include
open-wrap cliffs) and $r^\star$ the same seed without cliffs
(\texttt{generate\_sound\_ideal}). After bake $y=f_\theta(r)$, tile both cycles
$N{=}16$ periods:
\begin{align}
R &= \mathrm{clamp}\!\left(
  1 - \frac{\mathrm{rms}(y_{\mathrm{tiled}}-r^\star_{\mathrm{tiled}})}
           {\max(\mathrm{rms}(r^\star_{\mathrm{tiled}}),\varepsilon)},
  \,0,\,1\right).
\end{align}
Soft shape gate: if $\mathcal{S}<0.97$ then meta-rank uses $0.45\,R$, else $R$.
$\mathcal{D},\mathcal{S},Q$ remain report auxiliaries only.

\section{Bi-level / nested loss optimization}
Each trial samples a prior family, mutates $\theta$ near the previous frozen
init, then optionally runs inner coordinate descent on
$L=(1-\mathcal{D})+\lambda(1-\mathcal{S})$ over 48 fit seeds (steps
$0.15,0.07,0.03$). Families with \texttt{refine\_lambda} also try local $\lambda$
moves and a cheap re-sweep. \texttt{evo\_explore} is the mutate-only control
(zero inner sweeps). Outer ranking always uses residual $R$.

\section{Experiments}
\subsection{Protocol}
1500 trials; fast val 400 cycles; refine top-12 on 2000 cycles; five-algorithm
matrix vs naive DualCosine. Runtime $\approx 141$\,s release on the study machine.

\subsection{Results}
\begin{table}[t]
\centering
\caption{Residual-primary benchmark matrix (validation $N{=}2000$).}
\label{tab:matrix}
\begin{tabular}{llccc}
\toprule
Algorithm & Prior / kind & Residual & $\mathcal{S}$ & $Q$ \\
\midrule
Naive DualCosine & hand baseline & 0.698 & 0.995 & 0.789 \\
Meta Top~1 & evo\_explore & \textbf{0.824} & 1.000 & 0.784 \\
Meta Top~2 & evo\_explore & 0.823 & 1.000 & 0.785 \\
Meta Top~3 & evo\_explore & 0.823 & 1.000 & 0.785 \\
Meta Top~4 & evo\_explore & 0.821 & 1.000 & 0.785 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_benchmark_matrix.png}
\caption{Naive DualCosine vs Meta Top~1--4 under residual (primary), $\mathcal{D}$, $\mathcal{S}$.}
\label{fig:matrix}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_pareto_1500.png}
\caption{Elite residual vs unsupervised-loss proxy after 1500 trials.}
\label{fig:pareto}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/fig_prior_families.png}
\caption{Prior families among residual elites (top-20 / top-4).}
\label{fig:priors}
\end{figure}

Champion trial: \texttt{evo\_explore\_515}. Margin vs DualCosine:
$\Delta R \approx +0.126$. Production frozen residual before re-lock was
$\approx 0.718$; $\theta^\star$ is now locked to the champion.

\paragraph{Honest note on loss opt.}
All twenty Pareto-fast elites were \texttt{evo\_explore} (inner sweeps $=0$).
Bi-level / PBT / Bayes loss-opt priors were evaluated but did not win on residual.
Inner $L$-minimization remains part of the search design and is useful as a
nested regularizer for D/S-shaped landscapes; under prolonged residual, lighter
evolutionary mutations currently dominate.

\section{Discussion}
Residual makes the objective audible-prolonged rather than seam-local proxy.
The ``killer'' algorithm under this metric is wide evolutionary explore around a
strong init---not the deepest nested loss fit. That is a feature of the new
score, not a failure to search loss opt: the bilevel arm was present and lost
fairly.

\section{Limitations}
Residual uses a procedural ideal (no-cliff sibling), not a perceptual MOS.
Bake metrics $\neq$ listening tests. Nested loss budgets are small (48 seeds)
by design for 1500-trial throughput.

\section{Conclusion}
DenoiseOpt v2 ranks by prolonged residual and searches nested unsupervised loss.
Meta Top~1 (\texttt{evo\_explore\_515}) clearly beats DualCosine on residual
($0.824$ vs $0.698$) with perfect mid-cycle shape retention on the matrix.
Artifacts and reproduction scripts ship in this repository; frozen $\theta^\star$
ships in ReelSynth.

\section*{Repositories}
\begin{itemize}
  \item Synth: \url{https://github.com/reeldemo/reelsynth}
  \item Meta-learning paper/artifacts: \url{https://github.com/reeldemo/denoise-opt-meta}
\end{itemize}

\section*{Reproducibility}
\begin{verbatim}
# From reelsynth checkout:
cargo run -p reelsynth --release --bin bench_denoise_meta
python brand/artifacts/render_benchmark_matrix.py
# Or from this repo (figures already under paper/v2/figures):
cd paper/v2 && pdflatex -interaction=nonstopmode main.tex
\end{verbatim}

\begin{thebibliography}{99}

\bibitem{valimaki2006va}
V.~V\"alim\"aki and A.~Huovilainen,
``Oscillator and Filter Algorithms for Virtual Analog Synthesis,''
\emph{Computer Music Journal}, 30(2), 2006.

\bibitem{esqueda2016aliasing}
F.~Esqueda, S.~Bilbao, and V.~V\"alim\"aki,
``Aliasing Reduction in Clipped Signals,''
\emph{IEEE Trans.\ Signal Processing}, 2016.

\bibitem{esqueda2016blamp}
F.~Esqueda, V.~V\"alim\"aki, and S.~Bilbao,
``Rounding corners with BLAMP,''
\emph{Proc.\ DAFx}, 2016.

\bibitem{shan2021dwt}
S.~Shan et~al.,
``Differentiable Wavetable Synthesis,''
arXiv:2111.10003, 2021.

\bibitem{lehtinen2018n2n}
J.~Lehtinen et~al.,
``Noise2Noise: Learning Image Restoration without Clean Data,''
\emph{ICML}, 2018.

\bibitem{snoek2012bo}
J.~Snoek, H.~Larochelle, and R.~P.~Adams,
``Practical Bayesian Optimization of Machine Learning Algorithms,''
\emph{NeurIPS}, 2012.

\bibitem{bischl2023hpo}
B.~Bischl et~al.,
``Hyperparameter optimization: Foundations, algorithms, best practices\ldots,''
2023.

\bibitem{lopez2016irace}
M.~L\'opez-Ib\'a\~nez et~al.,
``The irace package: Iterated racing for automatic algorithm configuration,''
\emph{Operations Research Perspectives}, 2016.

\bibitem{zhang2007moead}
Q.~Zhang and H.~Li,
``MOEA/D: A Multiobjective Evolutionary Algorithm Based on Decomposition,''
\emph{IEEE Trans.\ Evolutionary Computation}, 2007.

\bibitem{finn2017maml}
C.~Finn, P.~Abbeel, and S.~Levine,
``Model-Agnostic Meta-Learning for Fast Adaptation of Deep Networks,''
\emph{ICML}, 2017.
\textit{(Screened.)}

\end{thebibliography}
\end{document}
