# DenoiseOpt / denoise-opt-meta — Constitution

Non-negotiable principles for Spec-Driven Development on this paper repo and its reelsynth experiment companions. Later artifacts (`requirements`, `design`, `tasks`, implement) must obey these.

1. **Honesty over hype** — no speech-enhancement or deep-SOTA claims without matching evidence.
2. **OA-only bibliography** for manuscript cites.
3. **Canonical metric truth** — ideal sibling = cliff-withheld procedural twin; no-bake ≠ ideal; maximize absolute $R$; DualCosine centering is PPO advantage only.
4. **Primary claim scope** — cycle-local wavetable seam restoration (DAFx/AES/cs.SD); CWRU/ECG are secondary classical-board pilots.
5. **Reproducibility** — fixed seeds, REPRO manifests, scripts in reelsynth; never wipe publishable `meta_approach_compare/`.
6. **Review completeness** — every weakness/Q/suggestion maps to a v8 location in `REVIEW_RESPONSE.md`.
7. **Cross-repo hygiene** — reelsynth scripts/artifacts + denoise-opt-meta paper; CHANGELOG when user-visible; mirror README points at current paper version.

Accepted: Gate A (user Build), 2026-07-24.
